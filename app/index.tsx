import { ActivityIndicator, View } from "react-native"; import { useEffect } from "react"; import { router } from "expo-router"; import { useQuery } from "@tanstack/react-query"; import { portal } from "@/lib/portal"; export default function Entry(){ const q=useQuery({queryKey:["me"],queryFn:portal.me,retry:false}); useEffect(()=>{if(q.isSuccess) router.replace(q.data?"/(tabs)":"/sign-in"); else if(q.isError) router.replace("/sign-in");},[q.isSuccess,q.isError,q.data]); return <View style={{flex:1,alignItems:"center",justifyContent:"center"}}><ActivityIndicator /></View>; }
