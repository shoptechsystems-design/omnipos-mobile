import { useState } from "react"; import { Alert, Pressable, Text, TextInput, View } from "react-native"; import { router } from "expo-router"; import { useColors } from "@/hooks/use-colors"; import { portal } from "@/lib/portal"; import { ScreenContainer } from "@/components/screen-container"; export default function SignIn(){ const colors=useColors(); const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [loading,setLoading]=useState(false); const submit=async()=>{try{setLoading(true);await portal.login({email,password});await portal.tenantContext();router.replace("/(tabs)");}catch(e){Alert.alert("Unable to sign in",e instanceof Error?e.message:"Please check your credentials");}finally{setLoading(false);}}; return <ScreenContainer className="px-5 justify-center" edges={["top","bottom","left","right"]}><View className="mb-10"><Text className="text-4xl font-bold text-foreground">Omni<Text style={{color:colors.primary}}>POS</Text></Text><Text className="text-base text-muted mt-2">Sign in to your live shop portal</Text></View><View className="rounded-3xl bg-surface border border-border p-5"><Text className="font-semibold text-foreground mb-2">Email</Text><TextInput value={email} onChangeText={setEmail} autoCapitalize="none" keyboardType="email-address" placeholder="you@business.com" placeholderTextColor={colors.muted} className="h-12 rounded-xl bg-background px-3 text-foreground mb-4"/><Text className="font-semibold text-foreground mb-2">Password</Text><TextInput value={password} onChangeText={setPassword} secureTextEntry placeholder="Your portal password" placeholderTextColor={colors.muted} className="h-12 rounded-xl bg-background px-3 text-foreground mb-5"/><Pressable disabled={loading||!email||!password} onPress={submit} style={{height:52,borderRadius:16,backgroundColor:loading||!email||!password?colors.border:colors.primary,alignItems:"center",justifyContent:"center"}}><Text className="text-white font-bold">{loading?"Signing in…":"Sign in"}</Text></Pressable></View></ScreenContainer>; }
