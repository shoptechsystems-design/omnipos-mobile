import { Pressable, Text, View } from "react-native";
import { router, useLocalSearchParams } from "expo-router";
import { ScreenContainer } from "@/components/screen-container";
import { IconSymbol } from "@/components/ui/icon-symbol";
import { useColors } from "@/hooks/use-colors";
import { money } from "@/shared/omnipos";
export default function CheckoutSuccess() { const colors = useColors(); const p = useLocalSearchParams<{ orderNumber: string; total: string; changeDue: string; points: string }>(); return <ScreenContainer className="px-5 justify-center" edges={["top", "bottom", "left", "right"]}><View className="items-center rounded-3xl bg-surface border border-border p-6"><View className="w-20 h-20 rounded-full items-center justify-center mb-4" style={{ backgroundColor: colors.primary }}><IconSymbol name="checkmark.circle.fill" size={48} color="#fff" /></View><Text className="text-2xl font-bold text-foreground">Sale complete</Text><Text className="text-muted mt-2">{p.orderNumber}</Text><View className="w-full rounded-2xl bg-background p-4 mt-6"><View className="flex-row justify-between mb-2"><Text className="text-muted">Total</Text><Text className="font-bold text-foreground">{money(Number(p.total))}</Text></View><View className="flex-row justify-between mb-2"><Text className="text-muted">Change due</Text><Text className="font-semibold" style={{ color: colors.primary }}>{money(Number(p.changeDue))}</Text></View><View className="flex-row justify-between"><Text className="text-muted">Loyalty points</Text><Text className="font-semibold text-foreground">+{p.points}</Text></View></View><Pressable onPress={() => router.replace("/")} style={{ height: 52, borderRadius: 16, backgroundColor: colors.primary, alignItems: "center", justifyContent: "center", width: "100%", marginTop: 20 }}><Text className="text-white font-bold">Start new sale</Text></Pressable><Pressable onPress={() => router.replace("/(tabs)/orders")} style={{ height: 48, alignItems: "center", justifyContent: "center", width: "100%" }}><Text style={{ color: colors.primary, fontWeight: "700" }}>View orders</Text></Pressable></View></ScreenContainer>; }
