import { useState } from "react";
import { Alert, FlatList, Modal, Pressable, Text, TextInput, View } from "react-native";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { ScreenContainer } from "@/components/screen-container";
import { portal } from "@/lib/portal";
import { useColors } from "@/hooks/use-colors";

export default function Inventory() {
  const colors = useColors();
  const client = useQueryClient();
  const q = useQuery({ queryKey: ["inventory"], queryFn: portal.inventory });
  const [selected, setSelected] = useState<(typeof q.data extends (infer T)[] | undefined ? T : never) | null>(null);
  const [adjustment, setAdjustment] = useState("");
  const [reason, setReason] = useState("");
  const adjust = useMutation({ mutationFn: () => portal.adjustInventory({ productId: selected?.id ?? 0, adjustment: Number(adjustment) || 0, reason: reason.trim() || "Mobile stock adjustment" }), onSuccess: () => { setSelected(null); setAdjustment(""); setReason(""); client.invalidateQueries({ queryKey: ["inventory"] }); client.invalidateQueries({ queryKey: ["products"] }); Alert.alert("Inventory updated", "The stock adjustment is synchronized with the portal."); }, onError: (error) => Alert.alert("Adjustment failed", error.message) });
  const fieldStyle = { height: 48, borderRadius: 14, backgroundColor: colors.background, borderWidth: 1, borderColor: colors.border, paddingHorizontal: 14, color: colors.foreground, marginTop: 10 } as const;
  return <ScreenContainer className="px-4 pt-4"><Text style={{ color: colors.foreground, fontSize: 28, fontWeight: "900", letterSpacing: -0.6 }}>Inventory</Text><Text style={{ color: colors.muted, fontSize: 13, marginTop: 2, marginBottom: 16 }}>Live stock and adjustments</Text><FlatList data={q.data ?? []} keyExtractor={(x) => String(x.id)} refreshing={q.isFetching} onRefresh={() => q.refetch()} contentContainerStyle={{ gap: 10, paddingBottom: 24 }} renderItem={({ item }) => <View style={{ borderRadius: 18, backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border, padding: 15 }}><View className="flex-row justify-between"><View className="flex-1"><Text style={{ color: colors.foreground, fontWeight: "800", fontSize: 15 }}>{item.name}</Text><Text style={{ color: colors.muted, fontSize: 12, marginTop: 4 }}>{item.sku}</Text></View><Text style={{ color: item.stock < 5 ? colors.warning : colors.primary, fontWeight: "900", fontSize: 20 }}>{item.stock}</Text></View><View className="flex-row justify-between items-center mt-3"><View style={{ paddingHorizontal: 9, paddingVertical: 5, borderRadius: 12, backgroundColor: item.stock < 5 ? "#FFF5DF" : "#E8F7F3" }}><Text style={{ color: item.stock < 5 ? colors.warning : colors.success, fontSize: 11, fontWeight: "800" }}>{item.stock < 5 ? "Low stock alert" : "Healthy stock"}</Text></View><Pressable onPress={() => setSelected(item)} style={({ pressed }) => [{ paddingHorizontal: 12, paddingVertical: 8, borderRadius: 14, backgroundColor: colors.background, borderWidth: 1, borderColor: colors.border }, pressed && { opacity: 0.7 }]}><Text style={{ color: colors.primary, fontWeight: "800", fontSize: 12 }}>Adjust stock</Text></Pressable></View></View>} ListEmptyComponent={<Text style={{ color: colors.muted, textAlign: "center", paddingVertical: 50 }}>{q.isLoading ? "Loading inventory…" : "No inventory data"}</Text>} /><Modal visible={Boolean(selected)} transparent animationType="slide" onRequestClose={() => setSelected(null)}><View style={{ flex: 1, justifyContent: "flex-end", backgroundColor: "rgba(14,27,58,0.32)" }}><View style={{ backgroundColor: colors.surface, borderTopLeftRadius: 26, borderTopRightRadius: 26, padding: 20, paddingBottom: 28 }}><View className="flex-row justify-between items-center"><View><Text style={{ color: colors.foreground, fontSize: 21, fontWeight: "900" }}>Adjust stock</Text><Text style={{ color: colors.muted, marginTop: 3 }}>{selected?.name}</Text></View><Pressable onPress={() => setSelected(null)}><Text style={{ color: colors.muted, fontSize: 24 }}>×</Text></Pressable></View><TextInput value={adjustment} onChangeText={setAdjustment} keyboardType="numbers-and-punctuation" placeholder="Adjustment, e.g. 10 or -2" placeholderTextColor={colors.muted} style={fieldStyle} /><TextInput value={reason} onChangeText={setReason} placeholder="Reason" placeholderTextColor={colors.muted} style={fieldStyle} /><Pressable disabled={!adjustment || adjust.isPending} onPress={() => adjust.mutate()} style={({ pressed }) => [{ height: 54, borderRadius: 16, backgroundColor: adjustment ? colors.primary : colors.border, alignItems: "center", justifyContent: "center", marginTop: 16 }, pressed && { opacity: 0.85 }]}><Text style={{ color: "#FFFFFF", fontWeight: "900" }}>{adjust.isPending ? "Saving…" : "Save adjustment"}</Text></Pressable></View></View></Modal></ScreenContainer>;
}
